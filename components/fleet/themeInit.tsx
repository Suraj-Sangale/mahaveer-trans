// This file is intentionally left as a no-op.
// Theme initialization is now handled globally by next-themes ThemeProvider
// in app/layout.tsx — no manual localStorage or data-theme manipulation needed.
export default function ThemeInit() {
  return null;
}
